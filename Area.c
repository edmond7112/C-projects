#include <stdio.h> 
int main() {
     
    int l,w,Area;
    Area=l*w;
    printf("Enter the first number");
    scanf("%d",&l);
    printf("Enter the second number");
    scanf("%d",&w);
    Area=l*w;
    printf("The Area of A Rectangle is %d",Area);
    
    return 0;
}